지금 구축하신 **Smart Agent(Reasoning Agent)**가 얼마나 똑똑하게 동작하는지 테스트해볼 수 있는 **"예상 질문 리스트"**를 난이도와 테스트 목적별로 정리해 드립니다.

이 질문들을 하나씩 던져보면서 터미널의 **Reasoning Trace(생각의 흐름)**가 의도한 대로 흘러가는지 확인해보세요.

🟢 1단계: 기초 탐색 (Schema & Basic Query)
목적: 에이전트가 inspect_schema를 잘 쓰는지, 기본적인 SPARQL 문법(LIMIT, ORDER BY)을 잘 만드는지 확인.

"이 데이터베이스에 어떤 종류의 데이터(Class)들이 저장되어 있어?"

예상 행동: inspect_schema 호출 -> 클래스 목록(:Incident, :Malware 등) 요약 답변.

"가장 최근에 생성된 사건(Incident) 5개만 찾아서, 제목과 날짜를 표 형식으로 보여줘."

예상 행동: run_sparql 호출 (ORDER BY DESC(?date) LIMIT 5 사용).

"등록된 악성코드(Malware)는 총 몇 개야?"

예상 행동: run_sparql 호출 (SELECT (COUNT(?s) AS ?count) ...).

🟡 2단계: 관계 추적 (Multi-hop Query)
목적: 에이전트가 JOIN 쿼리를 작성하거나, 그래프 구조를 이해하고 있는지 확인.

"악성코드 'LockBit'가 사용하는 MITRE ATT&CK 기술(Technique)들을 모두 알려줘."

예상 행동: :Malware -> :implementsTechnique -> :Technique 연결 쿼리 작성.

"'Finance' 섹터(산업군)를 타깃으로 한 사건들이 주로 사용하는 악성코드 이름이 뭐야?"

예상 행동: :Incident(:sector "Finance") -> :usesMalware -> :Malware 연결.

"특정 CVE(취약점)를 악용하는 악성코드가 포함된 사건이 있어? 있으면 사건 제목을 알려줘."

예상 행동: :Vulnerability -> :Malware -> :Incident (3단 연결) 추론.

🔴 3단계: 불확실성 및 전역 검색 (Reasoning & Global Search)
목적: 정보가 불명확할 때 search_everywhere 도구를 사용하여 스스로 문제를 해결하는지 확인. (가장 중요한 테스트)

"IP 주소 '201.16.194.227'에 대한 모든 정보를 찾아줘. 어디에 있는지는 모르겠어."

예상 행동: 특정 테이블 조회를 시도하거나, 바로 search_everywhere를 호출하여 전체 검색.

"내 시스템에서 'info.zip'이라는 파일명이 발견됐어. 이게 어떤 사건과 연관있는지 확인해봐."

예상 행동: Incident나 Indicator에서 찾다가 실패 -> search_everywhere("info.zip") 실행 -> URL 지표(.../info.zip) 발견 -> 관련 사건 추적.

"해시값(Hash)이 'a1b2c3...'인 파일이 있어?" (실제로 없는 값 입력)

예상 행동: run_sparql -> 결과 없음 -> search_everywhere -> 결과 없음 -> **"DB 전체를 검색해 보았으나 해당 해시값과 일치하는 정보는 없습니다."**라고 정직하게 답변.

🟣 4단계: 에러 복구 (Self-Correction)
목적: 일부러 헷갈리는 질문을 던져서 에러를 유발하고, 스스로 고치는지 확인.

(오타 유발) "인시던트(Incident)들의 'creatdAt'(오타) 속성을 조회해줘."

예상 행동: 쿼리 실행 -> Fuseki 에러(속성 없음) -> Thought: "아, 속성명이 틀렸나보군. 스키마를 다시 보거나 createdAt으로 고쳐야지." -> 쿼리 수정 -> 성공.